# content brief — week of 2026-07-27 → 2026-08-02

franchise-only. anchored to this week's GSC (API pull, window 7/19–7/25, full page-detail intersection data) and the standing strategy (`reports/seo-system-audit-2026-06-05.md`, `reports/content-brief-2026-05-18.md`). all slugs checked against `BLOCKED_SLUG_PATTERNS` before listing. byline human, old-internet aesthetic, lowercase headings, no fake data, no "why it loses" framing, no offshore links.

**posture this week: diagnose the pipeline before adding more to its queue.** Three consecutive weeks of recommendations (politicians-june retirement, Hall of Filth #3, the nav-link structural fix) have gone unexecuted — not because they were wrong calls, but because `git log --grep="Article: auto"` shows **zero automated article commits since 2026-07-17**, a full 10 days, despite `daily-article.yml` being correctly scheduled and enabled. Specifying a fourth week of new pages into a pipeline that isn't currently landing anything would just be another carried item next Monday. **1 urgent ops flag, 1 carried retirement (repeated, now priority-escalated), 1 carried structural flag (repeated), 1 conditional new page, 4 rest.**

---

## URGENT — diagnose `daily-article.yml` silence (not a content spec — needs Actions log access, i.e. James or a session with `gh auth`)

**Evidence:** last automated article commit is `a5e5233 Article: auto 2026-07-17`. The workflow file (`.github/workflows/daily-article.yml`) is unchanged, still has `schedule: cron: '30 9 * * *'` and `workflow_dispatch`, and isn't disabled. `ci_guarded_commit.sh` only produces a commit message containing "Article: auto" when there's a staged diff — so either (a) the job has been failing at the `claude -p` step or the guarded-commit step every day for 10 days, or (b) it's running clean but generating a page that gets fully excluded by the noindex guard every time (unlikely to happen 10 days straight), or (c) the scheduled trigger itself stopped firing (GitHub disables `schedule` triggers after 60 days of *repository* inactivity, which isn't the case here — board-update commits are landing multiple times daily — but a workflow-level issue, like a YAML parse error introduced upstream or an expired/rotated secret, would look identical to silence from outside).

**This session cannot diagnose further** — no `gh auth` available in this sandbox, so the Actions run history/logs are invisible here. **Recommend:** check the `daily-article` workflow's run history in the Actions tab; if every run is red, the failure step will say which of `ANTHROPIC_API_KEY`, `claude` CLI install, or `ci_guarded_commit.sh` is throwing. The `Telegram failure alert` step (`if: failure()`) should have already pinged the ops channel on each failed run — worth checking whether those alerts were sent and missed, or whether they themselves didn't fire (which would point at something wrong earlier in the job, like the checkout or Claude Code install step).

**Confidence: High** that no commits landed; **Low** on cause without log access. **This is the single highest-leverage item this week** — it blocks all three carried content items below.

---

## mon 7/27 — IMPROVE (carried 3rd week, HIGH priority): retire the politicians-june intent-mismatch page

- **Problem (High confidence, unchanged):** `/politicians-with-prediction-markets-june-2026/` still holds "polymarket popular active markets june 2026" (2 imp/pos 11.5, weekly; 28-day fading 270→20→9) and "polymarket popular markets june 2026" (1 imp/pos 10.0) at 0% CTR. Neither the July nor August "weirdest-active" edition has picked up any of this family — both page-detail files are still empty.
- **Changes (unchanged from the last two briefs):** set `"noindex": true` + `"noindex_reason"` on `politicians-with-prediction-markets-june-2026.json` (CI-writable). **Separately:** add a `permanent: true` 301 in `vercel.json` for bare + trailing-slash → `/weirdest-active-polymarket-markets-august-2026/` (outside `ci_guarded_commit.sh` scope — needs a manual commit). Do both in the same sitting — noindex-without-redirect drops the residual traffic instead of transferring it, which is worse than doing nothing.
- **Why this is now priority-escalated rather than just repeated:** three weeks unexecuted is no longer "hasn't gotten to it yet" — it's evidence the mechanism that was supposed to execute it (an automated content session) isn't running at all (see URGENT item above). This item and the daily-article silence are almost certainly the same root cause.
- **Confidence:** High on the diagnosis; the fix itself is unchanged and still correct.

## tue 7/28 — IMPROVE (carried 2nd week): `/funny-polymarket-bets/` — hold, do not re-edit

- **Update from this week's data:** position moved 13.5 → 9.0, the first improvement in three reports. But impressions also dropped 31% (75→52) in the same window the whole site's impressions dropped 9% — the GSC report reads this as demand fluctuation, not a content-driven recovery (nothing on the page changed this week). **Do not treat this as validation of any specific lever.**
- **Change:** still hold — no copy/metadata edit. The standing hypothesis (stall is inbound-authority-driven, fix is the nav/footer link below) is untested because the structural fix hasn't shipped. Re-verify position again next Monday with a second week of data before drawing any conclusion either way.
- **Confidence:** Low on what caused this week's position move; High that another copy edit this week would be premature.

## wed 7/29 — structural flag (carried 2nd week, not a content spec): franchise pages still have zero inbound links from homepage/board/nav

Re-confirmed this run: `grep -n "crazy-kalshi-bets\|funny-polymarket-bets" generate.py` returns **zero matches** — `nav_html()` and `footer_info_nav()` still don't reference either franchise page. Recommendation unchanged from last week: add a small promo/link line (e.g. under the board or in the footer nav) pointing at `/crazy-kalshi-bets/` and `/funny-polymarket-bets/`, same old-internet aesthetic. This is a `generate.py` edit outside `generate_content.py`'s scope — flagging again for a dev/code session to size and land. **Confidence: Medium** this is the fix for funny-polymarket's stall, unchanged from last week (still unproven, still untried).

## thu 7/30 — NEW (conditional on the pipeline actually running this week): Hall of Filth single-market deep dive #3

Same spec as the last two briefs, carried forward because it was never produced, not because the editorial case changed:

- **Working title:** pick the strongest currently-live, named, US-relevant market from that day's board with a genuine weird/outrageous hook.
- **Slug:** `hall-of-filth/{specific-market-slug}` (named-market, passes the guardrail automatically).
- **Format:** G (historical_story / Hall of Filth).
- **Primary keyword:** the named-market query itself.
- **Hero guidance:** one market, real odds, the $1 framing, genuine backstory. Avoid subjects already covered (Whitesides, Lear/deBessonet, Obama-charges, Monet, Trump-2016, GameStop, Buster Douglas, Leicester City, Red Sox 2004, Appalachian State, Miracle on Ice, US House member/Iran, Taylor Swift/Pope, NASA moon, biggest-parlay/longshot roundups, strangest-bets-ever-made, what-would-1-dollar-have-paid).
- **Internal links:** the platform-matching franchise page and `/hall-of-filth/`.
- **Editorial note:** same-day pick from the live board, no fabricated numbers. **Note:** the Monet page (7/16) still has zero GSC signal at 12 days old, both weekly and 28-day — worth a look before shipping a fourth page into the same silent-signal pattern, though 12 days isn't unusual for a low-volume named-market page.
- **Confidence:** High on format fit; Medium on whether adding volume matters more than fixing the pipeline that would ship it.

## fri 7/31 — REST

## sat 8/01 — REST

## sun 8/02 — REST

Three rest days is deliberate — repeating the same three unexecuted items a fourth time without first understanding why they didn't land would be noise, not signal. If the pipeline gets fixed mid-week, the carried items above are ready to execute immediately; no need to wait for next Monday.

---

## success criteria for this week (falsifiable)

- **At least one `Article: auto` commit lands this week** (any day) — the actual test of whether the pipeline issue is fixed. *This supersedes all editorial success criteria below; if this fails, none of the content items below can be scored fairly next week either.*
- **Politicians-june retirement fully executed** (both noindex commit AND vercel.json redirect, same sitting) — third week carried, checked by URL Inspection + direct redirect check next week.
- **Franchise clicks ≥ 51** (hold this week's level, not last week's higher 61 — avoiding a target that's disconnected from the demand-fluctuation read in this week's report). `/crazy-kalshi-bets/` holds ≥ 45 clicks.
- **`/funny-polymarket-bets/` position holds ≤ 10** (test whether this week's 9.0 was noise or a real floor) — not a recovery bar, just "didn't slide back."
- **If a Hall of Filth #3 ships:** reaches 200/indexable by end of week — gate only, not itself a success signal.

Not a success metric: total impressions (still subject to demand fluctuation, not a strategy signal on its own this week). Not a goal: further metadata edits to either franchise page — both audited again this run, nothing left to add.

---

## Deploy check — 2026-07-27 (read-only)

HTTP status (`curl -L`, live):

**All franchise + carried pages LIVE (200):**
- `/crazy-kalshi-bets/` ✅
- `/funny-polymarket-bets/` ✅
- `/polymarket-vs-kalshi-craziest-markets/` ✅
- `/weirdest-active-polymarket-markets-august-2026/` ✅
- `/hall-of-filth/monet-auction-record-bet/` ✅
- `/hall-of-filth/george-whitesides-ca-27-primary-bet/` ✅
- `/politicians-with-prediction-markets-june-2026/` ✅ (retirement candidate, still live pending the noindex + redirect)
- `/craziest-kalshi-markets` ✅ (still resolves through its confirmed 308 → `/crazy-kalshi-bets/`)

No new franchise pages shipped this week to check (see the pipeline-silence flag above) — this is itself the notable deploy-check finding, not a gap in the check.

**GSC URL Inspection deep links (for carried pages, if/when retirement or new pages ship):**
- Politicians-june (pending retirement): https://search.google.com/search-console/inspect?resource_id=sc-domain%3Adollarbets.lol&id=https%3A%2F%2Fwww.dollarbets.lol%2Fpoliticians-with-prediction-markets-june-2026%2F
- August active-markets edition: https://search.google.com/search-console/inspect?resource_id=sc-domain%3Adollarbets.lol&id=https%3A%2F%2Fwww.dollarbets.lol%2Fweirdest-active-polymarket-markets-august-2026%2F
